#ifndef _PATTERN_MANAGER
#define _PATTERN_MANAGER

void Sort_characters(char sorted[], unsigned int indexes[], char letters[], unsigned int length);
/*
    Rewrites 'sorted' array with 'letters' characters into 'indexes' locations.
*/

#endif